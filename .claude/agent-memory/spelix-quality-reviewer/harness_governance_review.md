# Harness governance procedural-coherence reviews (project)

**Dominant failure mode: outcome-enum drift across ship-loop SKILL.md steps.** SKILL.md is a state machine spread over numbered steps + a trailing inline cleanup note + an end-of-run summary table. When a step adds NEW terminal outcomes, three places must stay in sync: (1) the step that produces the outcome, (2) step 8 "Cleanup — runs for EVERY outcome" (needs a branch per outcome), (3) the "End of run" summary-table enum on the last line. Review method: enumerate every outcome the new text can produce, then confirm each has a step-8 cleanup branch AND a summary-enum slot.

**#239 approval-gate (2026-06-10) → FAIL (3 MEDIUM, 1 LOW), fixed in-loop.** Gate added "Skip (leave PR open)" and "Close PR & abandon" outcomes; step 8 only handled Merged + Deferred/blocked. Sharpest issue: step 7.6 inline note authorized `discard_changes` on close while step 8 said "never pass discard_changes blind" — mutually exclusive, with the only authorization living in an aside rather than the executing cleanup section. Fix pattern: per-outcome rules live ONLY in step 8; the Closed branch is the sole sanctioned `discard_changes: true` use, justified by the recorded human gate choice. Summary enum extended to merged/needs-human/blocked/skipped/closed.

**Clean patterns — don't re-flag:** "supersedes in part" + naming the exact behavior + bidirectional cross-link is the correct ADR supersession pattern. Dropping an ADR body `Status:` line is fine when the Decision Index row carries status. ship-loop is `disable-model-invocation: true`; /groom is the only autonomous caller and the gate's headless/no-response→needs-human fallback covers it. T3 gate-ordering living in the skill but not governance.md's T3 paragraph is acceptable asymmetry (governance defines tiers, the skill defines mechanism).
